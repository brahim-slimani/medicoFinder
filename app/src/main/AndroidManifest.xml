<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slimani.medicofinder">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/icon2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCNTh02Fki3h4lUytd7wiMwngC4Kt1n9XQ" />

        <activity android:name=".activities.IndexActivity" android:colorMode="default">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.Form1Activity" android:windowSoftInputMode="adjustPan" android:label="MedicoFinder"/>
        <activity android:name=".activities.Form2Activity" android:label="MedicoFinder"/>
        <activity android:name=".activities.Form3Activity" android:label="MedicoFinder"/>
        <activity android:name=".activities.MapsActivity" android:label="MedicoFinder"/>

    </application>

</manifest>